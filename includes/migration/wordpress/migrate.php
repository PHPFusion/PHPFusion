<?php

include "wordpress_multisite.php";

// do Rimelek's connection to the target Database

// then do a query, Left join via ID of User_name to Our's User_name. (User ID) is different.

// run while and inside -- insert into PHP-Fusion native table.
